Can you create a html mockup for a calendar site that tracks employee availability? It will be based off of shadcn/ui, I created the preset --preset b3lV3g3HO as a starting point with dark mode enabled by default. There will be a sidebar for the menu as well as the list of employees as a key. Each employee will have their own color to be able to easily glance at the calendar and know which employee is available on which days. The calendar will be a grid view, sunday-saturday like a regular calendar except the weeks will be split by hours, so any event will be like a progress bar starting at the start time/day of the project and going to the end time/day of the project. by default, projects start and end at noon with the option to adjust the time. The calendar only tracks taken space so if there is free space, its assumed the employee is available. The employees events will be stacked, with the priority being the same order it is on the sidebar (you should be able to re-order the employees) and the color of the event bar will be the employee's set color. The event options should be 'unavailable' and the name of the project, with the ability for multiple employees to be assigned to the same project for all or part of it. When creating a project, it should ask the start/end time/dates and it will show a timeline of employee availability below where you can assign employees by dragging and dropping or editing the start/end time/day of the employee. The calendar should also be filterable by employee, so you can hide/show events for specific employees only. I want all the front end components to be shadcn/ui including all date/range pickers. Please ask me any questions you may have to get a better understanding of this and keep in mind this is just a demo for now that we will build/deploy on railway if approved.

Great concept — before I start building, I have a few questions to make sure I get the layout and behavior right:
Calendar Layout (most important to clarify)
1. When you say "weeks split by hours" — are you picturing:
	- Option A: A weekly view (7 columns = Sun–Sat) where each column has hour rows going down, and events are vertical bars spanning a time range within a day — like Google Calendar's week view
	- Option B: A monthly grid where each row is a week (7 day columns), and within each day cell there are stacked horizontal bars representing events across hours — more of a Gantt-style horizontal timeline
	- Option C: Something else entirely?
2. What time range should the day show — business hours only (e.g. 8am–6pm), or full 24 hours?
3. Is there just the week view, or also a month overview for navigation?
The Preset
4. The --preset b3lV3g3HO — is that from the shadcn/ui CLI themes page (ui.shadcn.com/themes), a custom JSON, or something else? I want to make sure I reference the right CSS variables. If it's a custom preset you've set up, can you share the color tokens or a screenshot of what it looks like?
Employees & Projects
5. Roughly how many employees should the demo show? (Affects sidebar and stacking density)
6. For the "Unavailable" event type — does that just block out the employee's time with their color but labeled "Unavailable," or is it styled differently (e.g. hatched/dimmed) to distinguish it from a project assignment?
7. When a project spans multiple days in the week view — should the bar break and resume each day (like a calendar event wrapping), or flow continuously across day columns like a true Gantt bar?
Answers:
1: Something different, each row is essentailly a timeline split by hour of the week, hard vertical lines seperate the days and soft dotted lines show noon of each day, so the leftmost of the day cell is midnight and the rightmost is 11:59pm.
2: It's a full 24 hour day.
3: It is a month view but it would just show every week of the month with the ability to scroll to see ahead or behind.
4: It was shadcn's create feature that uses theming. mor about theming is found here https://ui.shadcn.com/docs/theming
5: for now, 3 employees.
6: It should be the employees color but muted. It's a way for employees to put days they're unavailable so they can give a reason as to why or leave blank for privacy, their choice but the look should be different from standard projects.
7: It should flow continuosly, if it flows through multiple weeks it would just end on the first week and continue on the second but the two events are treated as one. Maybe a project could be an outlined container with the project name and inside is the bar of the employees and the time they'll be on the project.
One other thing to keep in mind is you can click on events and a popup will display details.
Also, any project that goes into friday evning to end of day sunday would show a 'Weekend Fee' notification